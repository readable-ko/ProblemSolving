#include <bits/stdc++.h>
#define SETTING ios::sync_with_stdio(0), cin.tie(0)
using namespace std ;

int main()
{
    SETTING ;

    int N ;
    cin >> N ;

    N >>= 2 ;

    for(int i = 0 ; i < N ; i++)
        cout << "long " ;
    
    cout << "int\n" ;
}
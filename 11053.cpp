#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N, max_arr = 0 ;
    cin >> N ;
    int arr[N+1] ;
    for(int i = 1 ; i <= N ; i++)
    {
        cin >> arr[i] ;
    }

    int dp[1001] = {0,};

    // 각 숫자를 기점으로 순열을 만들어봄.
}
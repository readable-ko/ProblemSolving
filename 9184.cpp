#include <bits/stdc++.h>
using namespace std ;
int a, b, c ;

void dp()
{
    
}

int main()
{
    while(1)
    {
        cin >> a >> b >> c ;
        if(a == -1 && b == -1 && c == -1) break ;

        dp() ;
    }
}